<%
  song_authors = @song ? @song.authors.map(&:name).join(", ") : nil
%>

<% content_for(:title, [@song&.name, song_authors].compact_blank.join(" — ").presence || "Spotsby") %>

<%= render "players/player", song: @song %>
